spring:
  datasource:
    url: ${SURVDEMO_DB_URL}
    username: ${SURVDEMO_DB_USERNAME}
    password: ${SURVDEMO_DB_PASSWORD}
    hikari:
      data-source-properties:
        encrypt: true
        trustServerCertificate: true

# The local certificate bypass is forbidden in shared and Azure environments.